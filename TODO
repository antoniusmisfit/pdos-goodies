# TODO

* Modify PRINTF/FPRINTF to recognize and expand environment variables, using ${VARNAME} syntax.

* Add IF, WHILE, and FOR branching and looping commands.

* Add a command to set an environment variable from user input.

*Add a WHEREIS or WHICH command that searches the PATH for a specified command name.